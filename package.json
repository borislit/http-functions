{
  "name": "http-functions",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "postinstall": "WEBPACK_RESOLVE_SYMLINKS=true lerna bootstrap --hoist --no-ci",
    "release": "lerna run release"
  },
  "devDependencies": {
    "lerna": "^3.3.2"
  }
}
